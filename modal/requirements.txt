# =============================================================================
# Overmind Orchestrator — Production Dependencies
# =============================================================================
# Curated for ECS Fargate deployment. Removed ~2.5GB of unused packages
# (torch, sentence-transformers, transformers, scipy, scikit-learn, pillow).
#
# fastembed uses ONNX Runtime for inference — torch is NOT needed.
# Supabase PostgreSQL works via standard asyncpg — no special SDK needed.
# boto3 is included for AWS SDK access (SSM Parameter Store, future S3/DynamoDB).
# =============================================================================

# --- Web framework + server ---------------------------------------------------
fastapi==0.135.1              # ASGI framework — orchestrator endpoints
starlette==0.52.1             # fastapi dependency (ASGI toolkit)
uvicorn==0.34.3               # ASGI server — runs the FastAPI app
h11==0.16.0                   # uvicorn HTTP/1.1 parser

# --- Data validation ----------------------------------------------------------
pydantic==2.12.5              # Request/response models, agent schemas
pydantic_core==2.41.5         # pydantic C extension
annotated-types==0.7.0        # pydantic dependency
typing-inspection==0.4.2      # pydantic dependency
typing_extensions==4.15.0     # backport of typing features

# --- HTTP client --------------------------------------------------------------
httpx==0.28.1                 # Async HTTP client — LLM health checks, agent network tool
httpcore==1.0.9               # httpx transport layer
h2==4.3.0                     # HTTP/2 support for httpx
hpack==4.1.0                  # h2 dependency (HPACK header compression)
hyperframe==6.1.0             # h2 dependency (HTTP/2 framing)
certifi==2026.2.25            # TLS root certificates
idna==3.11                    # Internationalized domain names
sniffio==1.3.1                # Async library detection
anyio==4.12.0                 # Async compatibility layer (httpx, openai)

# --- OpenAI SDK + Langfuse observability --------------------------------------
openai==2.26.0                # AsyncOpenAI client — LLM tool-calling loop
langfuse>=2.0.0               # LLM observability — traces, spans, generations
distro==1.9.0                 # openai dependency (OS detection)
jiter==0.13.0                 # openai dependency (fast JSON parsing)

# --- Database (PostgreSQL / Supabase) -----------------------------------------
asyncpg==0.31.0               # Native PostgreSQL driver — Supabase connection
                              # ssl="require" satisfies Supabase TLS requirement
                              # No Supabase SDK needed — it's standard PostgreSQL

# --- Embeddings (fastembed + ONNX) --------------------------------------------
fastembed==0.7.4              # BAAI/bge-large-en-v1.5 via ONNX (NO torch needed)
onnxruntime==1.24.3           # ONNX inference engine — fastembed backend
numpy==2.4.3                  # Vector math — average_vectors in codebase_indexer
flatbuffers==25.12.19         # onnxruntime dependency
protobuf==6.33.5              # onnxruntime dependency
packaging==25.0               # onnxruntime/fastembed/langfuse dependency (langfuse requires <26.0)
mmh3==5.2.1                   # fastembed dependency (MurmurHash)
py_rust_stemmers==0.1.5       # fastembed dependency (text stemming)

# --- Hugging Face Hub (model downloads) ---------------------------------------
huggingface_hub==1.6.0        # fastembed downloads models from HF Hub
filelock==3.25.2              # HF Hub dependency (model cache locks)
fsspec==2026.2.0              # HF Hub dependency (filesystem abstraction)
hf-xet==1.4.0                # HF Hub dependency (Xet storage backend)
requests==2.32.5              # HF Hub dependency (HTTP downloads)
charset-normalizer==3.4.5     # requests dependency
urllib3==2.6.3                # requests dependency
tqdm==4.67.3                  # HF Hub dependency (download progress bars)
PyYAML==6.0.3                 # HF Hub dependency (model config parsing)

# --- Code parsing (tree-sitter) -----------------------------------------------
tree-sitter==0.25.2           # AST parser — codebase_indexer chunk extraction
tree-sitter-python==0.25.0    # Python grammar
tree-sitter-typescript==0.23.2 # TypeScript + TSX grammars
tree-sitter-javascript==0.25.0 # JavaScript grammar
tree-sitter-go==0.25.0        # Go grammar

# --- AWS SDK ------------------------------------------------------------------
boto3==1.42.66                # AWS SDK — SSM Parameter Store, future S3/DynamoDB
botocore==1.42.66             # boto3 core (low-level AWS API)
jmespath==1.1.0               # boto3 dependency (JSON query language)
s3transfer==0.16.0            # boto3 dependency (S3 transfer utilities)
python-dateutil==2.9.0.post0  # botocore dependency
six==1.17.0                   # python-dateutil dependency

# --- Async utilities ----------------------------------------------------------
aiohttp==3.13.3               # async HTTP (fastembed/HF downloads)
aiohappyeyeballs==2.6.1       # aiohttp dependency
aiosignal==1.4.0              # aiohttp dependency
attrs==25.4.0                 # aiohttp dependency
frozenlist==1.8.0             # aiohttp dependency
multidict==6.7.1              # aiohttp dependency
propcache==0.4.1              # aiohttp dependency
yarl==1.23.0                  # aiohttp dependency

# --- Misc shared deps ---------------------------------------------------------
regex==2026.2.28              # fastembed tokenizer dependency
toml==0.10.2                  # config parsing
cbor2==5.8.0                  # fastembed/HF serialization
setuptools==82.0.1            # pkg_resources (runtime dependency for some libs)
